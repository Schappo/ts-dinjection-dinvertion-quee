import { sum } from './app';

test('adds 1 + 2 to equal 3', () => {
  const a = 1;
  const b = 2;
  expect(sum(a, b)).toBe(3);
});

test('adds 1 + 2 to equal 3', () => {
  const a = 1;
  const b = 2;
  expect(sum(a, b)).toBe(3);
});
